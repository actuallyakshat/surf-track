import type { ScreenTimeData } from "@/types/types"

const token = ""
export async function syncPreviousDataWithBackend(
  currentWeekNumber: number,
  screenTimeData: ScreenTimeData
): Promise<void> {
  //TODO: Implement syncing with backend
}
